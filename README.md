# Loan_Status_Prediction_
Loan Status Prediction using Machine Learning — A Python-based ML project that predicts whether a bank loan application will be approved or rejected. The model is built using a Support Vector Machine (SVM) with a linear kernel, trained on applicant demographic and financial data.
